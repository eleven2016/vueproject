// 引用模板
var menuData = require('../assets/menu.json')
var routes = []
menuData.forEach((item) => {
  item.subMenu.forEach((menu) => {
    routes.push({
      path: `/${menu.id}`,
      name: menu.name,
      component: () => import(`@/components/${menu.id}`)
    })
  })
})
// 配置路由
export default routes
